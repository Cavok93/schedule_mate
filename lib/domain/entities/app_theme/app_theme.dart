import 'package:flutter/material.dart';

class AppTheme {
  final int id;
  final String name;
  final ThemeData themeData;

  const AppTheme({
    required this.id,
    required this.name,
    required this.themeData,
  });
}
